// Data Types:
// String (Characters)
// Number (integers(Int), decimals(Float))
// Boolean (True or False)

let studant = ("Neemias");
let age = (17);
let money = (1850.49);
let pobre = true;

console.log (typeof (studant));
console.log (typeof (age));