// ⚠Desafio 14

// Crie uma função que reproduza a sequencia de Fibonacci
// Passe como argumento quantos números deseja reproduzir
console.log(fibonacci(3));
function fibonacci(numero) {
    if (numero === 0) return null;
    if (numero === 1) return 0;

    let fibonacciArray = Array[0, 1];
    console.log(String(fibonacciArray).length());
    
    // for (let i = 2; i <= numero; index++) {
    //     fibonacciArray.push(fibonacciArray);
        
    // }

}